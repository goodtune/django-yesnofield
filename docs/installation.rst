============
Installation
============

At the command line::

    $ easy_install django-yesnofield

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-yesnofield
    $ pip install django-yesnofield
